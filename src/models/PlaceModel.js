class PlaceModel {
    constructor(name, address, description, availableTime, img, reviews) {
      this.name = name;
      this.address = address;
      this.description = description;
      this.availableTime = availableTime;
      this.img = img;
      this.reviews = reviews;
    }
  }
  
  export default PlaceModel;